/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cdn.pixabay.com', "cloud.appwrite.io"], // Add the domains you want to allow here
      },
}

module.exports = nextConfig
